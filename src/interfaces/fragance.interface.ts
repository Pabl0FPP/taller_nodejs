export interface FraganceInput {
    name: string,
    description: string,
    color: string,
    image_url: string;
}

export interface FraganceInputUpdate {
    name: string,
    description: string,
    color: string,
    image_url: string;
}